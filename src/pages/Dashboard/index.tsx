import { Box } from '@mui/material';

const Dashboard = () => {
	return (
		<Box
			component={'main'}
			height={'100vh'}
			bgcolor={'black'}
			display={'flex'}
			alignItems={'center'}
		>
			<h1>Dashboard</h1>
		</Box>
	);
};

export default Dashboard;
